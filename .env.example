# Copy to .env (gitignored) and fill in.
# Used by src/llm/eval_batch_run.py.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: override Ollama target (default talks to home_mac via SSH tunnel)
# OLLAMA_HOST=http://127.0.0.1:11434
# OLLAMA_MODEL=gemma4:e4b
